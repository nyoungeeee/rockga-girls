# 콘텐츠 관리 규칙

이 프로젝트는 DB 없이 정적 데이터로 콘텐츠를 관리한다. 사용자가 모바일 ChatGPT/Codex에서 자연어로 콘텐츠 추가·수정·삭제를 요청하면 아래 규칙을 따른다.

## 기본 원칙

- 콘텐츠 원본은 루트의 `data.js`다.
- 단순 콘텐츠 변경 요청에서는 `index.html`, `app.js`, `styles.css`를 수정하지 않는다.
- 별도 DB, API, 관리자 페이지, CMS를 추가하지 않는다.
- 사용자가 제공하지 않은 중요한 사실(가격, 후기, 날짜, 링크 상태 등)을 추측해서 만들지 않는다. 단, 사용자가 구매 링크를 제공한 아이템은 해당 상품 페이지나 신뢰할 수 있는 판매처에서 확인한 현재 상품 정보로 보완할 수 있다.
- 기존 콘텐츠는 요청 없이 삭제하거나 의미를 바꾸지 않는다.
- 같은 항목이 이미 있는지 먼저 확인하고 중복이 의심되면 기존 항목을 갱신할지 새로 추가할지 판단한다.
- 콘텐츠 변경 후 `site.lastUpdated`를 작업일 기준 `YYYY.MM.DD` 형식으로 갱신한다.
- 가능한 경우 `npm run build`로 정적 빌드를 검증한다.

## 데이터 위치

모든 콘텐츠는 `data.js`의 `ARCHIVE_DATA` 안에 있다.

- `site`: 사이트 메타 정보
- `items`: 락페 아이템/쇼핑 링크/후기
- `restaurants`: 행사장 주변 맛집
- `rooms`: 파생 오픈카톡방 정보
- `transfers`: 티켓·숙소 양도 정보

## 자연어 요청 처리

사용자는 정확한 필드명을 몰라도 된다. 요청 내용을 읽고 가장 알맞은 배열에 반영한다.

예시:

- "아이템에 이거 추가해줘" → `items`
- "행사장 맛집에 이 네이버지도 링크 추가해줘" → `restaurants`
- "파생방 하나 추가해줘" → `rooms`
- "티켓 양도 글 올려줘" → `transfers`
- "이 양도글 완료 처리해줘" → 해당 `transfers` 항목의 `status`를 `양도완료`, `url`을 `#`로 변경
- "지난번 우비 후기 수정해줘" → 기존 `items` 항목을 찾아 요청된 필드만 수정

필수 정보가 일부 빠져 있어도 화면 렌더링에 문제가 없고 의미를 왜곡하지 않는 범위라면 안전한 빈 값으로 처리한다. 중요한 사실을 임의 생성해야만 저장 가능한 경우에는 추측하지 않는다.

## 아이템 링크만 받은 경우의 자동 처리

사용자가 락페 아이템에 대해 **구매 링크만 제공하거나 상품명 + 구매 링크만 제공한 경우**, 추가 질문 없이 가능한 범위에서 아래 작업을 모두 수행한다.

1. 구매 링크를 열어 최종 상품 페이지를 확인한다. 단축 URL이면 리다이렉트된 실제 상품 페이지까지 따라간다.
2. 상품명, 브랜드, 판매처, 현재 표시 가격, 대표 이미지, 상품 설명/특징을 확인한다.
3. `items` 스키마에 맞춰 `name`, `category`, `shop`, `price`, `url`, `review`, `points`, `date`를 작성한다.
4. `review`는 실제 사용 경험을 꾸며내지 않는다. 상품 페이지에서 확인 가능한 특징을 바탕으로 락페에서의 활용도를 설명하는 중립적인 소개 문구로 작성한다.
5. 대표 이미지는 외부 URL을 `data.js`에 직접 박아두는 대신, 가능한 경우 원본 상품 대표 이미지를 내려받아 `assets/`에 저장한다.
6. 저장한 이미지는 `new URL("./assets/파일명", import.meta.url).href` 형식으로 `image`에 연결한다.
7. 상품 페이지에서 이미지를 직접 확보할 수 없으면 공식 브랜드/공식 판매처에서 동일 상품의 대표 이미지를 찾는다. 다른 상품, 유사 상품, 검색 결과의 임의 이미지는 사용하지 않는다.
8. 정확한 동일 상품 이미지를 검증할 수 없을 때만 `image: ""`로 둔다. 이 경우 결과 보고에 이미지 확보 실패를 명시한다.
9. 가격은 작업 시점의 판매 페이지 표시 가격을 사용한다. 정상가와 할인가가 함께 있으면 실제 구매 가능한 현재 판매가를 우선한다. 회원 전용/쿠폰 적용가처럼 조건부 가격만 보일 경우 조건을 숨기지 않는다.
10. 상품 URL은 사용자가 준 구매 링크를 기본값으로 유지한다. 단축 링크라도 사용자가 준 링크가 정상 작동하면 그대로 보존한다.
11. 콘텐츠 및 이미지 반영 후 검증하고, 사용자가 별도로 PR 검토를 요청하지 않았다면 main에 반영하여 Vercel 자동 배포가 트리거되도록 한다.

즉, 앞으로 사용자는 아래처럼 링크만 줘도 된다.

```text
아이템에 추가해줘
https://example.com/product/123
```

이 요청은 상품 정보 조사 → 대표 이미지 확보 → `assets/` 저장 → `data.js` 작성 → 검증 → main 반영까지 하나의 작업으로 처리한다.

## 행사장 맛집 네이버지도 링크 자동 처리

사용자가 행사장 맛집에 대해 **네이버지도/네이버플레이스 링크만 제공하거나 행사명 + 네이버지도 링크를 제공한 경우**, 추가 질문 없이 가능한 범위에서 아래 작업을 수행한다.

1. 사용자가 제공한 네이버지도 링크를 열어 최종 장소 페이지를 확인한다. 단축 URL이면 실제 장소 페이지까지 따라간다.
2. 장소 페이지에서 상호명, 업종/카테고리, 대표 이미지, 대표 또는 인기 메뉴를 확인한다.
3. 메뉴는 실제 페이지에서 확인 가능한 항목 중 대표성이 높은 메뉴를 **3~5개**만 `menus`에 넣는다. 메뉴명을 추측해서 만들지 않는다.
4. 사용자가 행사명 또는 행사장을 함께 알려주면 `festival`, `venue`에 그대로 반영한다. 링크만 주어 행사장과의 연관성을 확정할 수 없으면 임의로 특정 행사명을 만들지 않는다.
5. 대표 이미지는 네이버 장소 페이지에서 해당 업장의 이미지임을 확인한 경우 사용한다. 가능하면 원본 이미지를 `assets/`에 저장하고 `new URL("./assets/파일명", import.meta.url).href` 형식으로 연결한다.
6. 정확한 대표 이미지를 안정적으로 확보할 수 없으면 `image: ""`로 둔다. 다른 가게나 유사 음식 이미지를 대신 사용하지 않는다.
7. `description`은 네이버 장소 페이지에서 확인 가능한 업종/특징을 짧게 정리한다. 실제 방문 후기처럼 표현하지 않는다.
8. `url`에는 사용자가 제공한 네이버지도 링크를 유지한다. 화면의 카드를 클릭하면 이 URL을 새 탭으로 연다.
9. 같은 네이버 장소 또는 동일 상호가 이미 등록되어 있으면 중복 추가하지 않고 기존 항목을 확인한다.
10. 콘텐츠 및 이미지 반영 후 검증하고, 사용자가 별도로 PR 검토를 요청하지 않았다면 main에 반영하여 Vercel 자동 배포가 트리거되도록 한다.

예시:

```text
전주얼티밋 행사장 맛집에 추가해줘
https://naver.me/...
```

이 요청은 네이버 장소 확인 → 상호명/대표 이미지/주력 메뉴 3~5개 확인 → `restaurants` 작성 → 검증 → main 반영까지 하나의 작업으로 처리한다.

## items 스키마

```js
{
  id: "unique-kebab-case-id",
  name: "상품명",
  category: "카테고리",
  image: "",
  shop: "구매처",
  price: "가격",
  url: "https://...",
  review: "후기 또는 상품 특징 기반 소개",
  points: ["특징1", "특징2"],
  date: "YYYY.MM.DD",
}
```

규칙:

- `id`는 기존 값과 겹치지 않는 영문 소문자 kebab-case로 만든다.
- 구매 링크가 제공된 아이템은 대표 이미지 확보를 우선 시도한다.
- 이미지가 없거나 정확한 동일 상품 이미지임을 검증할 수 없을 때만 `image: ""`로 둔다.
- 이미지 파일은 가능하면 `assets/`에 저장하고 기존 형식인 `new URL("./assets/파일명", import.meta.url).href`를 사용한다.
- `date`는 게시/추가 날짜이며 `YYYY.MM.DD` 형식을 사용한다.
- `points`는 사용자 설명 또는 판매 페이지에서 확인한 특징에서 짧게 추출한다. 확인되지 않은 내용을 만들어내지 않는다.
- 사용자가 직접 사용 후기를 주지 않았다면 `review`에 실제 사용한 것처럼 표현하지 않는다.
- 새 항목은 기본적으로 배열 상단에 추가해 최신 콘텐츠가 먼저 보이게 한다.

## restaurants 스키마

```js
{
  id: "unique-kebab-case-id",
  festival: "행사명",
  venue: "행사장 또는 지역",
  name: "상호명",
  category: "음식점 카테고리",
  image: "",
  description: "짧은 소개",
  menus: ["메뉴1", "메뉴2", "메뉴3"],
  url: "https://naver.me/...",
  date: "YYYY.MM.DD",
}
```

규칙:

- `id`는 기존 값과 겹치지 않는 영문 소문자 kebab-case로 만든다.
- `menus`는 네이버 장소 페이지에서 확인한 실제 메뉴 중 3~5개만 넣는다.
- 대표 이미지가 확인되면 우선 확보하고, 검증할 수 없을 때만 `image: ""`로 둔다.
- 카드의 링크는 사용자가 준 네이버지도 URL로 연결한다.
- `festival` 또는 `venue`가 확인되지 않은 경우 추측하지 않고 빈 문자열로 둘 수 있다.
- 새 항목은 기본적으로 배열 상단에 추가한다.

## rooms 스키마

```js
{
  name: "방 이름",
  description: "방 설명",
  url: "https://open.kakao.com/...",
  note: "비고",
  active: true,
}
```

규칙:

- 운영 중이면 `active: true`, 종료·비활성 상태면 `false`.
- 상태 언급이 없으면 새로 공유된 방은 기본적으로 `true`로 본다.
- 링크가 제공되지 않았다면 임의의 오픈카톡 링크를 만들지 않는다.
- 새 항목은 기본적으로 배열 상단에 추가한다.

## transfers 스키마

```js
{
  type: "티켓",
  event: "행사명 또는 숙소명",
  date: "행사/숙박 날짜",
  quantity: "1매",
  price: "가격",
  status: "거래중",
  method: "거래 방법 또는 위치",
  url: "https://...",
  posted: "YYYY.MM.DD",
}
```

규칙:

- `type`은 `티켓` 또는 `숙소` 중 하나를 사용한다.
- 신규 양도 글은 별도 상태 언급이 없으면 `status: "거래중"`으로 둔다.
- 허용 상태는 `거래중`, `예약중`, `양도완료`다.
- 완료 처리 시 `status: "양도완료"`, `url: "#"`로 변경한다.
- `posted`는 `YYYY.MM.DD` 형식이다.
- 새 항목은 배열 상단에 추가한다.

## 수정 범위

사용자가 단순히 콘텐츠를 추가/수정/삭제해 달라고 한 경우 기본 변경 범위는 다음과 같다.

1. `data.js`
2. 아이템 구매 링크 또는 맛집 네이버지도 링크가 있으면 필요한 `assets/` 이미지

UI 변경을 명시적으로 요청하지 않았다면 다른 파일은 수정하지 않는다.

## 검증

콘텐츠 수정 후 다음을 확인한다.

1. `data.js` 문법이 유효한지 확인
2. 동일 id 또는 명백한 중복 항목이 없는지 확인
3. URL 문자열과 따옴표가 깨지지 않았는지 확인
4. 로컬 이미지 경로가 실제 `assets/` 파일과 일치하는지 확인
5. `site.lastUpdated`가 갱신됐는지 확인
6. 가능하면 `npm run build` 실행
7. main 반영 후 가능하면 실제 배포 페이지에서 새 콘텐츠와 이미지 노출을 확인

## 배포 규칙

- 사용자가 "추가해줘", "수정해줘", "지워줘"처럼 실제 콘텐츠 변경을 요청하면 단순 제안으로 끝내지 않고 저장소에 반영한다.
- 사용자가 PR 검토를 명시적으로 원하지 않는 한, 콘텐츠 변경은 검증 후 main까지 반영하여 Vercel 자동 배포가 트리거되게 한다.
- main 반영 또는 배포 상태 확인이 도구 권한상 불가능하면 그 단계만 명확히 보고한다.

## 결과 보고

작업 후 사용자에게 다음만 간단히 알려준다.

- 어떤 콘텐츠를 추가/수정/삭제했는지
- 링크 기반 자동 수집 항목(가격/이미지/메뉴 등)
- 변경한 파일
- 빌드/배포 검증 결과

단순 콘텐츠 수정에서는 불필요한 리팩터링을 하지 않는다.
